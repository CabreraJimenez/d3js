The goal of this project is creating bar graphs with gradient colors.

We will use the average temperature of Malaga for months during 2018 as data.

You only need to clone this code and continue those steps:

Open a terminal in the directory where the folder is cloned and execute npm i.
Then execute npm start if you want open a port in localhost.
Open a browser in the directory.

